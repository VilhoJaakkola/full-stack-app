enum Role {
    Admin = 'Admin',        // Admin is owner's role
    Driver = 'Driver',      // Driver is used by a person who drives the vehicle
    Customer = 'Customer' // Customer is company's role
}

export default Role; // This is the default export of the file